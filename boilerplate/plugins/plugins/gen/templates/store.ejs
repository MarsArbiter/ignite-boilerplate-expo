import {
  observable,
  action,
  computed
} from 'mobx'

export default class <%= props.name %>SStore {

  /*
  @observable props

  @computed get isValue() {
    return this.userID != null
  }

  @action.bound doAction() {
  }
  */
}
